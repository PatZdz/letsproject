import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Tutaj możesz umieścić swoje istniejące opcje konfiguracji Next.js
  // np. reactStrictMode: true,
};

export default nextConfig;